<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "読書管理" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body>
    <nav class="main-nav">
        <div class="nav-container">
            <div class="nav-brand">
                <%= link_to root_path, class: "brand-link" do %>
                <span class="brand-icon">読書管理</span>
                <% end %>
            </div>

            <div class="nav-menu">
                <% if logged_in? %>
                <%= link_to new_book_path, class: "nav-button primary" do %>
                <span class="button-icon">📚</span>
                書籍を追加
                <% end %>

                <%= form_with url: logout_path, method: :delete, local: true, class: "logout-form" do |form| %>
                <%= form.submit "ログアウト", class: "nav-button secondary" %>
                <% end %>
                <% else %>
                <%= link_to "ログイン", login_path, class: "nav-button primary" %>
                <%= link_to "新規登録", signup_path, class: "nav-button secondary" %>
                <% end %>
            </div>
        </div>
    </nav>

    <div class="page-container">
        <% if flash[:alert] %>
        <div class="flash alert">
            <span class="flash-icon">⚠️</span>
            <%= flash[:alert] %>
        </div>
        <% end %>
    </div>

    <main class="main-content">
        <%= yield %>
    </main>

    <footer class="main-footer">
        <div class="footer-container">
            <p class="footer-text">
                <span class="footer-icon">🐾</span>
                あなたがこれまで読んだ書籍を記録します。
            </p>
        </div>
    </footer>
</body>

</html>